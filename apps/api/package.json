{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "serve": "bun run dist/index.js",
    "build": "bun run bun.build.js"
  },
  "dependencies": {
    "hono": "^4.7.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}